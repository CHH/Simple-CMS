;; Config;; Api Keys are defined like this:;; ;; apikey[appName] = "someApiKey";;;; When the app does a POST, PUT or DELETE Request, the "apikey" parameter must be set in;; the request. The api key is the md5 hash of the app name and the api secret.api_secret[console] = "foobarbaz1234"