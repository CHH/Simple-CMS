;; This config file gets automatically parsed by the Plugin Loader
;; and is available as Config object through the getConfig() Method 
;; from within the Plugin.
;; 
;; If you want to know more about the syntax available in this file and 
;; the capabilities of the Config object, read the Documentation for 
;; Zend_Config and the parse_ini_file() function of PHP.
foo = "bar"
